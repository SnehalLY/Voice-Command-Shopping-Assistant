# Base URL of the backend API. In dev this is handled by Vite's proxy (/api -> localhost:4000).
# For production builds set this to your deployed backend URL, e.g.:
# VITE_API_BASE=https://voice-shopping-api.onrender.com
VITE_API_BASE=
