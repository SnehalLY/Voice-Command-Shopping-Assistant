# Server port
PORT=4000

# Comma-separated list of allowed CORS origins (frontend URL). Leave unset for dev (allow all).
# ALLOWED_ORIGIN=https://your-frontend.vercel.app

# Optional: override SQLite file location (defaults to ./data-store/shopping.db)
# DB_PATH=./data-store/shopping.db
